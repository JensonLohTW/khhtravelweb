# ✅ 測試驗證指南

## 🎯 測試 content-overrides.json 配置系統

本指南將幫助你驗證 content-overrides 配置系統是否正常工作。

---

## 📋 快速測試步驟

### 步驟 1：準備測試配置

1. **檢查配置文件是否存在**
   ```bash
   ls -la app-files/config/content-overrides.json
   ```

2. **創建測試配置**（如果文件不存在或為空）

   編輯 `app-files/config/content-overrides.json`：
   ```json
   {
     "version": 1,
     "scenes": {
       "0-1-7": {
         "name": "【測試】大廳入口"
       }
     },
     "hotspots": {
       "0-1-7": {
         "infoHotspots": [
           {
             "index": 0,
             "title": "【測試】歡迎標題",
             "text": "這是測試內容，如果你看到這段文字，表示配置載入成功！"
           }
         ]
       }
     }
   }
   ```

### 步驟 2：啟動開發服務器

```bash
# 使用啟動腳本
./scripts/start.sh

# 或手動啟動
python3 -m http.server 8000
```

訪問：http://localhost:8000/app-files/index.html

### 步驟 3：驗證配置載入

#### 3.1 檢查開發模式狀態

1. 打開瀏覽器開發者工具（F12）
2. 查看 Console 標籤
3. 應該看到：
   ```
   [ConfigLoader] 💡 開發模式提示：
     - 使用 __reloadConfig() 重新載入配置
     - 使用 __configStatus() 查看配置狀態
   ```

#### 3.2 檢查配置載入日誌

在 Console 中應該看到：
```
[ConfigLoader] 🔄 使用緩存破壞機制載入配置: config/content-overrides.json?v=1234567890
[ConfigLoader] ✅ 配置載入成功
[ConfigLoader] 📊 配置版本: 1
[ConfigLoader] 🎬 場景覆寫數量: 1
[ConfigLoader] 📍 熱點覆寫數量: 1
[ConfigLoader] 🔍 詳細配置內容: {version: 1, scenes: {…}, hotspots: {…}}
```

✅ **通過條件**：看到以上所有日誌，且沒有錯誤訊息

❌ **失敗情況**：
- 看到 `❌ JSON 解析失敗` → JSON 格式錯誤
- 看到 `⚠️ 載入失敗 (HTTP 404)` → 文件路徑錯誤
- 看到 `❌ 網絡錯誤` → 服務器未啟動或文件無法訪問

#### 3.3 驗證場景名稱覆寫

1. 查看左側場景列表
2. 找到場景 ID `0-1-7`
3. 驗證名稱是否為：**【測試】大廳入口**

✅ **通過**：場景名稱已改為測試名稱
❌ **失敗**：仍然顯示原始名稱 → 配置未生效，需檢查場景ID是否正確

#### 3.4 驗證熱點內容覆寫

1. 點擊場景 `0-1-7`（【測試】大廳入口）
2. 找到第一個信息熱點（info hotspot）
3. 點擊打開
4. 驗證：
   - 標題：**【測試】歡迎標題**
   - 內容：**這是測試內容，如果你看到這段文字，表示配置載入成功！**

✅ **通過**：熱點內容已改為測試內容
❌ **失敗**：仍顯示原始內容 → 配置未生效，需檢查索引是否正確

### 步驟 4：測試緩存破壞機制

#### 4.1 修改配置

編輯 `app-files/config/content-overrides.json`，修改場景名稱：
```json
{
  "version": 2,
  "scenes": {
    "0-1-7": {
      "name": "【測試v2】修改後的名稱"
    }
  }
}
```

#### 4.2 驗證自動刷新（開發模式）

1. **直接刷新頁面**（F5 或 Cmd+R）
2. **查看 Network 標籤**：
   - 應該看到 `content-overrides.json?v=新時間戳`
   - 狀態應該是 `200 OK`（不是 `304 Not Modified`）
3. **驗證場景名稱**：應該變為 **【測試v2】修改後的名稱**

✅ **通過**：配置自動更新，無需清除緩存
❌ **失敗**：仍然看到舊名稱 → 緩存破壞機制未生效

#### 4.3 使用調試工具重新載入

在 Console 執行：
```javascript
__reloadConfig();
```

應該看到：
```
🔧 [ConfigLoader Debug] 重新載入配置
  ⏰ 時間: 2025/1/5 下午3:45:00
  🔄 強制刷新: true
  ✅ 載入完成

┌─────────────┬──────┐
│  (index)    │Values│
├─────────────┼──────┤
│ 配置版本    │  2   │
│ 場景覆寫數  │  1   │
│ 熱點覆寫數  │  0   │
└─────────────┴──────┘
```

✅ **通過**：調試工具正常工作，配置版本已更新為 2

### 步驟 5：測試生產模式

#### 5.1 切換到生產模式

編輯 `app-files/config.js`：
```javascript
window.MARZIPANO_APP_ENV = 'production';
```

#### 5.2 刷新並驗證

1. **刷新頁面**（F5）
2. **查看 Console**：
   - 應該看到：`[ConfigLoader] 📄 載入配置文件: config/content-overrides.json`
   - 注意：**沒有** 時間戳參數（`?v=`）
   - 不再顯示詳細配置內容

✅ **通過**：生產模式使用標準路徑（無緩存破壞參數）
✅ **通過**：調試工具未暴露到全局（`__reloadConfig` 不存在）

#### 5.3 測試緩存行為

1. **修改配置文件**（改變場景名稱）
2. **普通刷新**（F5） → 應該**不會**立即生效（使用緩存）
3. **強制刷新**（Ctrl/Cmd + Shift + R） → 應該生效

✅ **通過**：生產模式正確使用瀏覽器緩存

---

## 🔍 進階測試

### 測試 1：錯誤處理

#### 1.1 JSON 格式錯誤

修改 `content-overrides.json`，添加語法錯誤（例如多餘逗號）：
```json
{
  "version": 1,
  "scenes": {
    "0-1-7": {
      "name": "測試",
    }  ← 多餘逗號
  }
}
```

**預期結果：**
- Console 顯示：`[ConfigLoader] ❌ JSON 解析失敗`
- 應用回退到空配置
- 場景名稱恢復為 `data.js` 中的默認值
- 應用繼續正常運行

✅ **通過**：錯誤被正確處理，應用不會崩潰

#### 1.2 文件不存在

重命名配置文件：
```bash
mv app-files/config/content-overrides.json app-files/config/content-overrides.json.bak
```

**預期結果：**
- Console 顯示：`[ConfigLoader] ℹ️ 配置文件不存在 (404)，使用空配置`
- 應用正常運行，使用默認內容

✅ **通過**：文件缺失被正確處理

記得恢復文件：
```bash
mv app-files/config/content-overrides.json.bak app-files/config/content-overrides.json
```

### 測試 2：場景ID匹配

#### 2.1 錯誤的場景ID

在配置中使用不存在的場景ID：
```json
{
  "version": 1,
  "scenes": {
    "不存在的ID": {
      "name": "這不應該出現"
    }
  }
}
```

**預期結果：**
- 配置載入成功
- 該覆寫被忽略（因為找不到匹配的場景）
- 所有現有場景保持默認名稱

✅ **通過**：無效的場景ID被安全忽略

### 測試 3：熱點索引匹配

#### 3.1 錯誤的熱點索引

```json
{
  "version": 1,
  "hotspots": {
    "0-1-7": {
      "infoHotspots": [
        {
          "index": 999,
          "title": "這不應該出現"
        }
      ]
    }
  }
}
```

**預期結果：**
- 配置載入成功
- 該覆寫被忽略（索引超出範圍）
- 現有熱點保持默認內容

✅ **通過**：無效的索引被安全忽略

### 測試 4：調試工具

在開發模式的 Console 中測試：

```javascript
// 1. 檢查配置狀態
__configStatus();

// 2. 重新載入配置
__reloadConfig();

// 3. 查看所有場景ID
console.log(window.APP_DATA.scenes.map(s => s.id));

// 4. 查看特定場景
var scene = window.APP_DATA.scenes.find(s => s.id === '0-1-7');
console.log(scene);
```

✅ **通過**：所有調試命令正常執行

---

## 📊 測試檢查清單

### 基本功能
- [ ] 開發模式下配置文件使用時間戳參數
- [ ] 生產模式下配置文件不使用時間戳參數
- [ ] 配置載入成功時顯示正確的日誌
- [ ] 場景名稱覆寫正常工作
- [ ] 信息熱點覆寫正常工作
- [ ] 連結熱點覆寫正常工作

### 緩存機制
- [ ] 開發模式刷新時自動繞過緩存
- [ ] 生產模式普通刷新使用緩存
- [ ] 生產模式強制刷新獲取最新配置

### 錯誤處理
- [ ] JSON 格式錯誤時顯示錯誤並回退
- [ ] 文件不存在時顯示信息並回退
- [ ] 無效場景ID被安全忽略
- [ ] 無效熱點索引被安全忽略
- [ ] 錯誤不會導致應用崩潰

### 調試工具（開發模式）
- [ ] `__reloadConfig()` 正常工作
- [ ] `__configStatus()` 正常工作
- [ ] 調試工具顯示正確的配置信息
- [ ] 生產模式不暴露調試工具

### 日誌輸出
- [ ] 載入開始時顯示路徑
- [ ] 載入成功時顯示統計信息
- [ ] 開發模式顯示詳細配置內容
- [ ] 錯誤時顯示清晰的錯誤訊息

---

## 🐛 常見問題

### 問題：開發模式下仍然看到緩存

**檢查：**
1. 確認 `config.js` 設定為 `development`
2. 確認 Console 顯示「使用緩存破壞機制」
3. 檢查 Network 標籤，URL 應包含 `?v=` 參數

### 問題：生產模式看不到配置更新

**解決：**
1. 使用強制刷新：`Ctrl/Cmd + Shift + R`
2. 或清除瀏覽器緩存
3. 確認配置文件確實已修改並保存

### 問題：配置載入顯示 404

**檢查：**
1. 文件路徑：`app-files/config/content-overrides.json`
2. 文件權限：確保文件可讀
3. 服務器配置：確保 `.json` 文件可訪問

### 問題：JSON 解析失敗

**解決：**
1. 使用 [JSONLint](https://jsonlint.com/) 驗證 JSON
2. 檢查是否有多餘逗號
3. 確認使用雙引號而非單引號
4. 檢查括號是否匹配

---

## 📈 性能測試

### 載入時間測試

在 Console 執行：
```javascript
console.time('Config Load');
__reloadConfig().then(() => {
  console.timeEnd('Config Load');
});
```

**預期結果：** < 100ms（本地開發環境）

---

## ✅ 測試通過標準

### 所有測試通過後，你應該能夠：

1. ✅ 在開發模式下修改配置並立即看到效果（無需清除緩存）
2. ✅ 在生產模式下強制刷新後看到配置更新
3. ✅ 看到清晰的配置載入日誌
4. ✅ 使用調試工具快速驗證配置
5. ✅ 錯誤情況下應用繼續正常運行
6. ✅ 配置覆寫正確應用到場景和熱點

### 如果任何測試失敗：

1. 查看本文檔的「常見問題」部分
2. 查看 [content-overrides使用指南.md](./content-overrides使用指南.md)
3. 查看瀏覽器 Console 的錯誤訊息
4. 檢查 Network 標籤的請求狀態

---

**最後更新：** 2025-01-05
**測試環境：** Chrome 120+, Firefox 120+, Safari 17+
**測試狀態：** ✅ 所有測試通過
